{
  "name": "rentease-root",
  "version": "1.0.0",
  "description": "Root management for RentEase Frontend and Backend",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && cd Frontend && npm install && cd ../Backend && npm install",
    "dev:frontend": "cd Frontend && npm run dev",
    "dev:backend": "cd Backend && npm run dev",
    "dev": "npx concurrently \"npm run dev:frontend\" \"npm run dev:backend\" --names \"FRONTEND,BACKEND\" --prefix-colors \"cyan,magenta\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
